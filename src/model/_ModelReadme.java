package model;

/**
 * ─── CAPA MODEL ─────────────────────────────────────────────────────────────
 *
 * Aquí van las clases POJO (Plain Old Java Objects) que representan
 * las entidades de tu proyecto.
 *
 * Convenciones:
 *  - Una clase por entidad (ej. Usuario, Producto, Orden, etc.)
 *  - Solo atributos privados, constructor vacío, constructor con parámetros,
 *    getters y setters.
 *  - No contienen lógica de negocio ni acceso a BD.
 *
 * Ejemplo de estructura:
 * ┌─────────────────────────────────────────────┐
 * │  package model;                             │
 * │                                             │
 * │  public class Entidad {                     │
 * │      private int id;                        │
 * │      private String nombre;                 │
 * │                                             │
 * │      public Entidad() {}                    │
 * │      public Entidad(int id, String nombre)  │
 * │          { this.id=id; this.nombre=nombre; }│
 * │                                             │
 * │      // getters y setters...                │
 * │  }                                          │
 * └─────────────────────────────────────────────┘
 *
 * Este archivo es solo documentación, no tiene código funcional.
 */
class _ModelReadme { /* Solo sirve para que la carpeta tenga un archivo Java */ }
